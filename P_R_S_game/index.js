
function solve(pickedNumber) {
    let screenValue = document.querySelector('.screen_detail').innerHTML;
    console.log(screenValue);
    console.log(pickedNumber);
    if (screenValue === 0) {
        console.log(screenValue);
        console.log(pickedNumber);
    } else {
        
    }
}